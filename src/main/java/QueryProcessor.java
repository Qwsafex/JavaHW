import com.sun.istack.internal.NotNull;

class QueryProcessor {
    byte[] process(@NotNull byte[] data) {
        throw new UnsupportedOperationException();
        /*
        switch (queryType) {
            case 0: {
                return ("Hello, " + path).getBytes();
            }
            default: {
                throw new UnsupportedOperationException();
            }
        }
        */
    }
}
